import { motion } from "motion/react";
import { Button } from "../ui/button";
import { Card } from "../ui/card";

export const AninmatedButton = motion.create(Button);
export const AninmatedCard = motion.create(Card);
